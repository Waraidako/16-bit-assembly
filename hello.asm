booba for an error
argoomba
; Some comment

   ; Some comment with spaces before it

mov a, 16
mov 16, a
mov b, [a + 8]
mov [234a9b], 29348

and c
and 3289
and [130]
and [g]
and [b + 439]
and [439 + b]
and [c + d]

or d

shr
shl

inc a
inc c
dec a
dec c